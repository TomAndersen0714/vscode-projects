// https://www.w3schools.com/js/js_loop_for.asp
let names = ["Tom", "Alise", "Jim"];

for (let i = 0; i < names.length; i++) {
    console.log("cars.at(i) = " + (names.at(i)));
    console.log("cars[i] = " + (names[i]));
}